char data = 0;
void setup() {
  Serial.begin(9600); 
}
void loop() {
 if (Serial.available()>0)
  {
    char servopos=Serial.read();
    char servopos1=Serial.read();
    char realservo= servopos;
    Serial.print(realservo);
    if(realservo>=1 && realservo<=5)
    {
      Serial.println("servo1 is on!!");
    }
   
  }
}
